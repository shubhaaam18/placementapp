<!DOCTYPE html>
<html lang="en">
<head>
    <title>Sign Up</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background: url('https://entrackr.com/storage/2022/10/Coding-Ninjas.jpg') center center fixed;
            background-size: cover;
            overflow: hidden; /* Prevent background from scrolling */
        }

        .blur-container {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            backdrop-filter: blur(5px); /* Adjust the blur value as needed */
        }

        .container {
            background-color: rgba(255, 255, 255, 0.9); /* Background color with opacity */
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 300px;
            text-align: center;
            display: flex;
            flex-direction: column;
            position: relative;
        }

        h4 {
            color: #333;
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: #555;
        }

        input {
            width: calc(100% - 16px);
            padding: 8px;
            margin-bottom: 16px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        button {
            background-color: #4caf50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }

        .login {
            text-decoration: none;
            color: #2196F3;
            margin-top: 16px;
            align-self: center; /* Center the link horizontally */
        }

        .login:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="blur-container"></div>
    <div class="container">
        <h4>Sign Up</h4>

        <form action="/users/create" method="post">

            <label for="name">Name</label>
            <input type="text" name="name" placeholder="John Doe" required />

            <label for="email">Email</label>
            <input type="email" placeholder="abc@email.com" name="email" required />

            <label for="password">Password</label>
            <input type="password" name="password" required />

            <label for="confirmPassword">Confirm Password</label>
            <input type="password" name="confirmPassword" required />

            <button type="submit">Sign Up</button>
        </form>

        <a class="login" href="/users/signin">Log in Instead</a>
    </div>
</body>
</html>
