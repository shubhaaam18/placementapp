<!DOCTYPE html>
<html lang="en">
<head>
    <title>Sign In</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            position: relative;
            overflow: hidden;
        }

        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://entrackr.com/storage/2022/10/Coding-Ninjas.jpg') center center fixed;
            background-size: cover;
            filter: blur(5px); /* Adjust the blur value as needed */
        }

        form {
            position: relative;
            z-index: 2; /* Adjust the z-index to ensure the form appears above the pseudo-element */
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 300px;
            text-align: center;
        }

        h4 {
            color: #333;
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: #555;
        }

        input {
            width: 100%;
            padding: 8px;
            margin-bottom: 16px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        button {
            background-color: #4caf50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }

        a {
            text-decoration: none;
            color: #2196F3;
        }

        a:hover {
            text-decoration: underline;
        }

        div {
            margin-top: 16px;
        }
    </style>
</head>
<body>
    <form action="/users/create-session" method="POST">
        <h4>Sign In</h4>

        <!-- Email input -->
        <div>
            <label for="email">Enter Email</label>
            <input type="email" id="email" placeholder="abc@email.com" name="email" required />
        </div>

        <!-- Password input -->
        <div>
            <label for="password">Enter Password</label>
            <input type="password" id="password" name="password" required />
        </div>

        <!-- Submit button -->
        <button type="submit">Sign in</button>

        <!-- Register buttons -->
        <div>
            <a href="/users/signup">Register</a>
        </div>
    </form>
</body>
</html>
